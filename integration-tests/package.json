{
  "name": "@peerbot/integration-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun test",
    "test:watch": "bun test --watch",
    "test:full": "./run-tests.sh",
    "mock:slack": "bun run src/mocks/slack-server.ts",
    "mock:claude": "bun run src/mocks/claude-server.ts"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.0.0"
  }
}
